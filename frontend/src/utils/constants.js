/**
 * Frontend-wide constants shared across pages, guards and services.
 * Mirrors backend/src/utils/constants.js.
 */

export const ROLES = Object.freeze({
  ADMIN: 'admin',
  CLIENT: 'client',
  CANDIDATE: 'candidate',
});
