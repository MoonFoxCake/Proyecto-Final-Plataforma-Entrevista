{
  "name": "nexo-perfil-backend",
  "version": "0.1.0",
  "private": true,
  "description": "API backend de Nexo Perfil (Express + Firebase Admin SDK, desplegado como Cloud Function)",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon --watch src --exec node src/index.js",
    "start": "node src/index.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "firebase-admin": "^12.1.1",
    "firebase-functions": "^4.9.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": "20"
  }
}
